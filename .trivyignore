# glibc: Integer overflow in memalign leads to heap corruption
# No fix available upstream (status: affected) - suppress until Debian publishes a patch
CVE-2026-0861
CVE-2025-62727

# --- OS packages: no upstream fix available (status: affected / fix_deferred) ---
# util-linux family (bsdutils, libblkid1, libmount1, libsmartcols1, libuuid1, mount, login, liblastlog2-2)
CVE-2026-53615   # util-linux: integer overflow in libblkid dos.c
CVE-2026-41992   # gzip: global buffer overflow in LZH decompression
CVE-2026-54369   # acl (libacl1): symlink traversal privilege escalation
CVE-2025-69720   # ncurses (libncursesw6, libtinfo6, ncurses-base/bin): buffer overflow, possible code execution
CVE-2026-42496   # perl-archive-tar: path traversal via crafted symlinks (CRITICAL)
CVE-2026-8376    # perl: heap buffer overflow compiling regex on 32-bit builds
CVE-2026-42497   # perl-archive-tar: arbitrary file modification via crafted hardlinks
CVE-2026-48962   # perl-io-compress: code execution via attacker-controlled output glob
CVE-2026-9538    # perl-archive-tar: DoS via crafted tar header

# --- starlette: fixed in 1.1.0 / 1.3.1, upgrade currently blocked by FastAPI 0.115.6 pin ---
CVE-2026-48818   # starlette: SSRF and NTLM credential theft via UNC paths in StaticFiles
CVE-2026-54283   # starlette: form-limit bypass enabling DoS
